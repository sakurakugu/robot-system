# AGENTS.md — AI 编码助手配置

> 本文件为 AI 编码助手提供项目规范和工作流指引。

---

## 项目概览

机器狗控制系统，包含以下子项目：

| 目录                                 | 技术栈                                     | 说明               |
| ------------------------------------ | ------------------------------------------ | ------------------ |
| `app/robot-phone/`                   | React Native 0.84 + TypeScript             | 手机端 App         |
| `app/robot-cloud/前端/`              | Vue 3 + TypeScript + Vite                  | 云端管理前端       |
| `app/robot-cloud/后端/`              | Node.js + TypeScript + Express + WebSocket | 云端后端服务       |
| `app/robot-agent/robot-agent/`       | Python 3.10+                               | 机器狗本体代理程序 |
| `app/robot-agent/robot-server/`      | Python 3.10+ + FastAPI                     | 机器狗配置服务器   |
| `app/robot-agent/sparkrobot-common/` | Python 3.10+                               | 公共库             |

---

## 通用原则

### 开发阶段约定

- **项目处于开发阶段，允许完整重构，无需向前兼容。**
- 前端页面均为热更新，修改代码后无需重启服务，直接刷新即可验证。
- 消除已有的冗余代码、过时注释和无用的向后兼容逻辑。

### 代码注释

- **所有注释一律使用中文。**
- 仅对非显而易见的逻辑添加注释，不要为自解释代码添加废话注释。
- 函数/类的 docstring 同样使用中文。

### 代码风格

- 不要过度工程化：只实现当前需求，不为假想的未来需求设计抽象。
- 不要添加用户未要求的功能、配置项或错误处理分支。
- 保持解决方案的最小复杂度。
- 但做计划时可以考虑未来需求，设计时可以适当预留接口，并使用破坏性重构的思路。

---

## TypeScript / JavaScript 项目

### 代码质量检查（每次修改后必须执行）

```bash
# 在对应子项目目录下执行
npm run lint
npm run typecheck
```

确保两条命令均无错误后，才算完成任务。

---

## Python 项目

### 代码质量检查（每次修改后必须执行）

```bash
# 在对应子项目目录下执行
ruff check .
mypy . --fix
```

确保两条命令均无错误后，才算完成任务。

### 规范

- 使用 Python 3.10+ 语法，善用类型注解。
- 遵循 `pyproject.toml` 中已配置的 ruff/mypy 规则。

---

## 工作流

1. 理解需求后，先阅读相关文件，再进行修改。
2. 修改完成后，在对应子项目目录执行 lint 和 typecheck。
3. 所有回复使用**中文**。
