### 需求

1. 找一找能不能往机器狗上配激光雷达

2. 写机器狗教学内容

3. 现在先是群控、编舞，然后是ai优化、等等，然后是人脸识别、物品识别

4. 手机端如果关闭了对话页面，之前的聊天记录不会储存

5. 改一下动作的提示词

6. https://grpc.org.cn/docs/what-is-grpc/introduction/
   让机器人端通过 grpc 连接到后端的 python 服务，然后把结果返回给 nodejs 服务

7. 让大模型调用工具而不是通过提示词来行动
   https://bailian.console.aliyun.com/cn-beijing/?spm=5176.29597918.J_SEsSjsNv72yRuRFS2VknO.2.55ed7b08Zvf6Yi&tab=doc#/doc/?type=model&url=2862208

8. 到时候支持将 robot-server 的账号密码发送到服务端（cloud）进行保存，然后手机端如果尝试密码错误后，从服务器获取最新的密码，防止改了密码后，手机端登录不了

9. 关闭 sdk 模式后，可以尝试让手机端通过智元自带的来控制移动

10. 实现 `last_action_before_disable` 功能
    - 当前 `application.py` 中定义了 `last_action_before_disable` 变量但未实际使用
    - 设计目的是记录关闭 SDK 模式时机器狗的最后动作状态
    - 需要在关闭 SDK 模式前记录当前动作，重新开启时恢复该状态

11. 群控问题:
    1. 如果调整了移动距离等的时间，执行时，应该根据执行自动计算中间要留多少时间让它执行，或者显示警告要当前时间过短，可能无法执行成功这一步/下一步
    2. 服务端下载更新时，可以检测与本地的时间戳，防止不知道是否有更新，或者用自增版本号
    3. 将那些原版丑陋的弹窗改成自定义的
    4. 编舞时，新增机器人/狗只是一个占位符，要添加对应的映射才是正确的，而不是直接从机器人列表中指定机器人。当然这样也可以，只不过最好把他抽离出来。
    5. 然后一个动作轨道对应一个机器人/狗的动作，动作轨道可以复制，还有动作轨道的可以改名？
    6. 手机中机器狗映射可以保存，而不是退出后映射就消失了

12. 游客模式不允许访问服务器（或者只是登记一些信息/云控？）

13. 实现让机器狗通过视觉识别走向一个物品，现在走向不精确

# 次要需求

> 或者说较难做的需求

1. 前端观看视频，使用手机端推送给后端，然后后端转发给前端（mediasoup），推送机器狗的视频流不是用手机自带的摄像头（这个尝试了好久，效果都不好）
   1. 前端改成从手机发送 webrtc 视频流而到服务端（不是发送图片）
   2. 然后手机端在设置里面添加一个 switch，是否允许将视频流转发到服务端，默认开启（当然要服务器向手机端询问视频流时，手机端才向服务端（前端）发送）d:\elric\Code\Repos\robot-dog\app\robot-phone\RobotPhone\src\features\settings\screens\SettingsScreen.tsx
   3. 然后手机端向服务器发送时，会在手机端的机器人操作的顶部显示向云端上传的图标 d:\elric\Code\Repos\robot-dog\app\robot-phone\RobotPhone\src\features\robots\screens\RobotOperationScreen.tsx
   4. 点击后在右边显示弹窗正在向云端发送视频流（使用点击动作然后在右边显示的弹窗）（就是把手机的通过后端转发到前端）

### 其他需求

还有文件内部的 TODO

修 Bug

## 九、红色目标自动接近并停车方案（推荐）

目标：用户说“向前走到前面的红色物品那里”，机器人自动识别红色目标并接近，在安全距离前稳定停车，不贴太近、不越过目标。

### 9.1 推荐总体方案

采用“视觉识别 + 闭环速度控制 + 双阈值停车 + 安全兜底”的最小可行架构：

1. **视觉检测层**：从 `rtsp://127.0.0.1:8554/test` 取流，识别画面中红色物体，输出目标框 `(cx, cy, w, h)`。
2. **控制决策层**：以目标框中心偏差控制转向，以目标框面积（或高度）估计距离并控制前进速度。
3. **执行层**：通过现有移动指令链路发送速度命令（`move/ai_move`），持续小步更新。
4. **停车层**：达到“停止距离阈值”后清零速度并保持锁定，防止继续前冲。
5. **安全层**：目标丢失、超时、角度过大时触发减速/停车，必要时急停。

### 9.2 为什么这是当前最适合的实现

- 现有系统已具备实时视频流（RTSP/WebRTC）和实时运动控制能力，不需要改动底层运控协议。
- 该方案不依赖深度相机，先用单目就能跑通“识别-接近-停车”核心闭环。
- 后续可平滑升级：把“红色阈值检测”替换成通用检测模型（如 YOLO），控制层无需重写。

### 9.3 控制逻辑（防止走太近/越过）

以图像宽度 `W`、目标框中心 `cx`、目标框面积占比 `r = box_area / (W*H)` 为核心量：

- **转向控制**：`yaw = k_yaw * ((cx - W/2) / (W/2))`，并限制在安全范围内（如 `[-0.25, 0.25]`）。
- **前进控制**：`vx = k_v * (r_stop - r)`，当目标远时 `vx` 大，接近时自动减小。
- **双阈值停车（带滞回）**：
  - `r >= r_stop_enter`：进入停车状态（`vx=0`）。
  - `r <= r_stop_exit`：才允许重新前进（`r_stop_exit < r_stop_enter`，避免抖动启停）。
- **禁止越过**：停车后默认仅允许原地微调朝向，不再给正向 `vx`，除非目标明显变远。

建议初始参数（先标定再微调）：

- `r_stop_enter = 0.10 ~ 0.14`
- `r_stop_exit = r_stop_enter - 0.02`
- `vx_max = 0.18 m/s`
- `vx_min = 0.04 m/s`
- `yaw_max = 0.25 rad/s`
- 控制频率：`10~15 Hz`

### 9.4 红色识别实现建议

第一阶段建议用 HSV 阈值法快速落地：

1. BGR 转 HSV；
2. 两段红色阈值掩码合并（低色相 + 高色相）；
3. 开闭运算去噪；
4. 取最大连通域或最大轮廓；
5. 过滤过小面积和异常长宽比目标。

推荐再加两条稳态规则：

- 连续 `N` 帧（如 3~5）都有效才认为“锁定目标”；
- 连续 `M` 帧（如 5~8）丢失则减速并停车。

### 9.5 安全与工程化细节

- **超时停车**：接近任务超过上限时长（如 15s）自动停车。
- **遥控优先级**：收到人工摇杆/急停后，视觉接近任务立即让权。
- **低电量降速**：电量低于阈值时收紧 `vx_max`。
- **日志记录**：每次任务记录 `r、vx、yaw、停车原因`，便于回放调参。

### 9.6 最小落地步骤（按当前仓库）

1. 在机器狗侧新增一个“红色目标接近”任务循环（读取视频帧、输出 `vx/yaw`）。
2. 复用现有移动指令发送通道，按 `10~15Hz` 下发小步速度命令。
3. 实现双阈值停车和目标丢失停车。
4. 在手机端新增一个语音/按钮入口，触发该任务并显示“搜索中/接近中/已停车”状态。
5. 先在固定光照下标定阈值，再补充不同距离与光照测试样例。

### 9.7 从“红色物品”升级到“垃圾桶/包/任意物体”

可以，推荐把“目标定义”从“颜色”改成“类别或文本目标”，控制与停车逻辑保持不变。

#### 9.7.1 推荐两阶段路线

1. **阶段A（快速可用）**：固定类别检测
   使用轻量目标检测模型（如 YOLO 系列）识别 `trash can / backpack / bottle ...`，输出目标框后直接复用 9.3 的接近与停车控制。
2. **阶段B（更通用）**：开放词汇检测
   输入文本目标（如“黑色垃圾桶”“红色背包”），由开放词汇检测模型返回候选框，再走同一套控制闭环。

#### 9.7.2 任务接口建议（统一上层能力）

建议统一为一个动作接口：

```json
{
  "type": "vision_follow_target",
  "target": "trash can",
  "stop_distance_level": "medium",
  "timeout_sec": 15
}
```

- `target`：目标类别或文本描述（如 `trash can`、`backpack`、`red bag`）。
- `stop_distance_level`：近/中/远停车档位，内部映射到不同 `r_stop_enter`。
- `timeout_sec`：超时保护，防止长时间找不到目标还持续运动。

#### 9.7.3 防止“识别错目标”的关键策略

- **目标选择规则**：同类多个框时优先“置信度高 + 距离中心近 + 面积稳定”的目标。
- **时序稳定**：只有连续多帧命中同一目标 ID 才执行前进。
- **重识别机制**：目标丢失先减速，短时重试；超时后停车并提示“目标丢失”。
- **安全边界**：任何时刻人工急停、摇杆输入都能立即打断自动接近。

#### 9.7.4 参数建议（类别化后）

- 小物体（瓶子/盒子）：`r_stop_enter` 取小一点（更靠近再停）。
- 大物体（垃圾桶/纸箱）：`r_stop_enter` 取大一点（更早停车）。
- 初期建议按“类别 → 停车阈值”做静态表，后续再改为在线自适应标定。

### 9.8 云端识别 + 本地跟踪的协同方案

可以这样做，而且这是很实用的架构：**云端负责“识别是什么”，本地负责“连续跟踪和控制”**。

#### 9.8.1 云端如何汇报“垃圾桶在哪里”

核心是返回**图像坐标系中的目标框**（bbox），而不是只返回“有垃圾桶”。

推荐最小上报字段：

```json
{
  "type": "vision_detection",
  "frame_id": 10231,
  "timestamp_ms": 1772000123456,
  "target": "trash can",
  "score": 0.91,
  "bbox": { "x": 0.42, "y": 0.33, "w": 0.21, "h": 0.4 },
  "image_size": { "w": 1920, "h": 1080 }
}
```

字段说明：

- `bbox` 推荐用**归一化坐标**（0~1），避免分辨率变化导致兼容问题。
- `x,y` 表示框左上角，`w,h` 表示宽高。
- `frame_id` 必须回传，用于和本地视频帧精确对齐。
- `score` 用于本地做置信度过滤（如低于 0.5 不接管）。

#### 9.8.2 本地如何接管跟踪

1. 本地收到云端 bbox 后，在对应帧初始化跟踪器（如 KCF/CSRT 或轻量检测器短周期重检）。
2. 后续每帧由本地跟踪器更新框位置，按 9.3 控制逻辑输出 `vx/yaw`。
3. 每隔 `N` 帧（如 10 帧）或目标不稳定时，再请求云端重识别做纠偏。

#### 9.8.3 防止延迟导致“框已过时”

- **时间戳门限**：`now - timestamp_ms > 300ms` 的检测结果不直接用于前进，只用于重定位。
- **帧号对齐**：优先使用同 `frame_id` 或最近邻帧，避免“上一秒的位置”驱动当前控制。
- **速度限幅**：云端刚回传目标时，先低速接近（如 `vx <= 0.08`），稳定后再提速。

#### 9.8.4 推荐握手流程

1. 本地请求：`请识别 target=trash can`（上传抽帧图或关键帧）。
2. 云端返回：`target + score + bbox + frame_id`。
3. 本地确认：`accepted/rejected`（低置信度可拒绝并重试）。
4. 本地进入跟踪接近态，持续输出状态：`searching/tracking/approaching/stopped/lost`。
