### 需求

1. 找一找能不能往机器狗上配激光雷达

2. 写机器狗教学内容

3. 群控、编舞，然后是ai优化、物品识别并走过去（能用但都一般般），然后人脸识别还没搞

4. 向后走 步数 是 -2 并没有向后走，而是向前走

5. 手机端和前端如果关闭了对话页面，之前的聊天记录不会储存

6. 改一下动作的提示词

7. https://grpc.org.cn/docs/what-is-grpc/introduction/
   让机器人端通过 grpc 连接到后端的 python 服务，然后把结果返回给 nodejs 服务 （感觉还是直接将edge音频的改成阿里的，然后直接改成nodejs更好）

8. 让大模型调用工具而不是通过提示词来行动
   https://bailian.console.aliyun.com/cn-beijing/?spm=5176.29597918.J_SEsSjsNv72yRuRFS2VknO.2.55ed7b08Zvf6Yi&tab=doc#/doc/?type=model&url=2862208

9. 到时候支持将 robot-server 的账号密码发送到服务端（cloud-server）进行保存，然后手机端如果尝试密码错误后，从服务器获取最新的密码，防止改了密码后，手机端登录不了

10. 关闭 sdk 模式后，可以尝试让手机端通过智元自带的来控制移动

11. 实现 `last_action_before_disable` 功能
    - 当前 `application.py` 中定义了 `last_action_before_disable` 变量但未实际使用
    - 设计目的是记录关闭 SDK 模式时机器狗的最后动作状态
    - 需要在关闭 SDK 模式前记录当前动作，重新开启时恢复该状态

12. 机器狗本体的服务加上火花前缀，防止和其他的重名

13. 群控问题:
    1. 如果调整了移动距离等的时间，执行时，应该根据执行自动计算中间要留多少时间让它执行，或者显示警告要当前时间过短，可能无法执行成功这一步/下一步
    2. 服务端下载更新时，可以检测与本地的时间戳，防止不知道是否有更新，或者用自增版本号
    3. 将那些原版丑陋的弹窗改成自定义的
    4. 编舞时，新增机器人/狗只是一个占位符，要添加对应的映射才是正确的，而不是直接从机器人列表中指定机器人。当然这样也可以，只不过最好把他抽离出来。
    5. 然后一个动作轨道对应一个机器人/狗的动作，动作轨道可以复制，还有动作轨道的可以改名？
    6. 手机中机器狗映射可以保存，而不是退出后映射就消失了

14. 记得把服务器 8554 端口开出来，否则前端无法连接到后端的视频推流

15. 现在云端的电量一直在0%和49%一直跳

# 次要需求

> 或者说较难做的需求

### 其他需求

还有文件内部的 TODO

修 Bug
